package dto

/*
import (
	"time"
)

func updateStringField(reqValue string, compField *string) {
	if reqValue != "" && *compField != reqValue {
		*compField = reqValue
	}
}

func updateTimeField(reqValue time.Time, compField *time.Time) {
	if !reqValue.IsZero() && *compField != reqValue {
		*compField = reqValue
	}
}

func updateBoolField(reqValue, compField *bool) {
	if reqValue != nil && *compField != *reqValue {
		*compField = *reqValue
	}
}

func updateIntField(reqValue int, compField *int) {
	if reqValue > 0 && *compField != reqValue {
		*compField = reqValue
	}
}
*/
